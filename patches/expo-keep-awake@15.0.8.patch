diff --git a/src/index.ts b/src/index.ts
index 069ac3b97f6dac28d7166dac611e5d9c66874064..b85d711f35018ce66da5b453cfc66961e4f5c089 100644
--- a/src/index.ts
+++ b/src/index.ts
@@ -35,6 +35,12 @@ export function useKeepAwake(tag?: string, options?: KeepAwakeOptions): void {
       if (isMounted && ExpoKeepAwake.addListenerForTag && options?.listener) {
         addListener(tagOrDefault, options.listener);
       }
+    }).catch((error) => {
+      if (!options?.suppressDeactivateWarnings) {
+        console.warn(
+          `expo-keep-awake could not activate for tag "${tagOrDefault}".`,
+          error
+        );
+      }
     });
     return () => {
       isMounted = false;
